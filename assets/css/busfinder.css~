*, *:before, *:after {
  -moz-box-sizing: border-box; -webkit-box-sizing: border-box; box-sizing: border-box;
 }

body, html, #map, #container {
	height: 100%;
	width: 100%;
	overflow: hidden;
}

body, html {
	margin:0;
	background: #ddd;
}

#map {
	position: absolute;
	top: 0;
	left: 0;
	z-index:0;
}

#container {
	padding: 20px;
	position: absolute;
	top: 0;
	left: 0;
	z-index: 500;
	pointer-events: none;
}

.header,
.menu span,
.breadcrumbs span,
.zoom-control div,
.view {
	pointer-events: auto;
	color: #333;
	font-family: "Lato", sans-serif;
	background: rgba(255,255,255,.9);
	padding: 10px;
	box-shadow: 5px 5px 0px rgba(0,0,0,.15);
  transition: height .2s, background .2s, color .2s, left .2s;
}

.view-wrapper {
  clear: both;
  position: relative;
}

.view {
  top: 0;
  left: -10px;
  position: absolute;
  display: none;
}

.header {
	width: 350px;
  margin-right: 10px;
  margin-bottom: 10px;
}

.navigation {
  float: left;
}

.zoom-control {
	position: absolute;
	top: 20px;
	right: 20px;
	width: 45px;
	font-size: 18px;
	font-weight: 900;
	text-align: center;
}

.hover:hover, .breadcrumbs span:hover, .zoom-control div:hover {
	cursor: pointer;
	background: rgba(100,255,255,.9);
	color: #055;
}

.menu {
	margin-bottom: 20px;
}

.menu span:hover {
	cursor: pointer;
	background: rgba(255,100,255,.9);
	color: #505;
}

.breadcrumbs span {
  font-size: 12px;
}

.breadcrumbs span, .menu span {
	display: inline-block;
	cursor: pointer;
	transition: background .25s, color .25s;
	margin-right: 10px;
}

.header img {
	width: 100%;
}

.title {
	font-size: 2.25em;
	font-family: "Source Sans Pro", sans-serif;
	font-weight: 200;
	color: rgba(0,150,150,1);
	margin-bottom: 10px;
}

.bus-view, .stop-view {
	display:none;
}

.view {
  display: none;
	width: 400px;
  overflow-y: auto;
  max-height: 410px;
}

.view .left {
	float: left;
	margin-bottom: 10px;
}

.view .right {
	float: right;
}

.view .both {
	clear: both;
	margin-bottom: 10px;
}

.view .label {
	display: block;
	font-size: 18px;
	font-weight: 200;
	color: rgba(150,0,150,1);
}

.view .data {
	font-weight: 300;
	font-size: 36px;
}

.view table {
	width: 100%;
	border-spacing: 0;
	border: 0;
}

.view td {
	padding: 5px 0;
}

.view tr {
	transition: background .25s;
}

.view tr:hover {
}

.search-view { 
	overflow: hidden;
	padding: 0;
	color: #333;
}

.search-wrapper {
	position: relative;
	height: 65px;
}

.search-label {
	position: absolute;
	top: 10px;
	left: 10px;
	font-size: 12px;
	transition: opacity .25s;
	opacity: 0;
}

.search {
	position:absolute;
	top: 15px;
	border: 0;
	font-family: "Lato", sans-serif;
	font-weight: 300;
	font-size: 24px;
	width: 100%;
	line-height: 24px;
	font-weight: 300;
	outline: none;
	padding: 10px;
}

.placeholder {
	position: absolute;
	top: 0px;
	left: 0px;
	font-family: "Lato", sans-serif;
	font-weight: 300;
	font-size: 34px;
	width: 100%;
	line-height: 34px;
	font-weight: 300;
	outline: none;
	padding: 14px 10px;
	pointer-events: none;
	transition: opacity .25s;
}

.results { 
	display: none; 
	opacity: 0;
	transition: opacity .25s;
  overflow-y: auto;
  max-height: 345px;
}

.results .label {
	padding: 10px;
}

.results .items div {
	padding: 10px;
	font-size: 20px;
	font-weight: 300;
	cursor: pointer;
	transition: background .25s;
}

.results .items div:hover {
	background: #eee;
}

.stops .items div, .recent-view .list div {
	font-size: 17px;
	cursor: pointer;
	transition: background .25s;
  padding: 5px 0;
}

.stops .items div:hover, .recent-view .list div:hover {
	background: #eee;
}
